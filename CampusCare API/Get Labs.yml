info:
  name: Get Labs
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/labs"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
